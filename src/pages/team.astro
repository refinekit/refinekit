---
import Layout from '~/layouts/Layout.astro'
import { getEntry, render } from 'astro:content'

// Load team page content from vault
const teamPage = await getEntry('pages', 'team')
const { Content } = teamPage ? await render(teamPage) : { Content: null }
const title = teamPage?.data.title || 'Team'
const subtitle = teamPage?.data.subtitle
---

<Layout title={title} description="Our team">
  <div class="relative z-[1]">
    <div class="fade-up">
      <div class="py-8 pb-10 px-6 sm:px-8">
        <h1 class="flex flex-col gap-2">
          <span class="text-4xl sm:text-5xl tracking-tight">
            {title}
          </span>
          {subtitle && (
            <span class="text-muted-foreground font-light text-xl sm:text-2xl block tracking-wide text-balance">
              {subtitle}
            </span>
          )}
        </h1>
        {Content ? (
          <div class="markdown prose prose-lg dark:prose-invert max-w-none mt-6">
            <Content />
          </div>
        ) : (
          <p class="text-muted-foreground mt-6">No team page content found. Create a team.md file in your vault.</p>
        )}
      </div>
    </div>
  </div>
</Layout>
